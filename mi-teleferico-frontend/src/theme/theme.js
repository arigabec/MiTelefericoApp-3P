import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#2e5ca2',
      },
      secondary: {
        main: '#3d3784',
      },
      verde: {
        main: "#359c2d",
      }
    },
  });
  
export default theme; 